-- ------------------------------------------------------------------------------
-- 此指令碼用於將 Pandas 的主資料庫升級到 2024.01.30 版本
-- 注意: 若存在更低版本且從未匯入的升級指令碼, 請按版本號從小到大依序匯入
-- ------------------------------------------------------------------------------

-- -----------------------------------------------
-- 儲存視野控制標記
-- -----------------------------------------------

ALTER TABLE `char`
	ADD COLUMN `view_control` BIGINT unsigned NOT NULL default '0' AFTER `disable_call`
;
